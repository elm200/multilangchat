## セキュリティとプライバシー

**想定している使い方は、身内で短時間立てるセルフホストです。** 恒常的に公開するサービスとしては
作られていません。以下は判断材料です。ご自分の使い方に照らして考えてください。

- **発言1件ごとに LLM の呼び出しが発生し、レート制限はありません。** 公開URLに置くと、
  誰かがメッセージを連投するだけで API クレジットが際限なく消費されます。
  **OpenRouter のキーに使用上限(credit limit)を設定したほうがいいかもしれません。** 仮に使いすぎても使用額を一定範囲に限定できます。
- **合言葉を設定しない場合、URLを知っている人は誰でも入室できます。** 合言葉を設定した場合は
  総当たり対策として、連続失敗時に全体の試行レートが制限されます(最大60秒)。
- **退室ボタンを押さずに接続が切れた人は、9つある部屋の枠を保持し続けます。** 長時間運用すると
  部屋が埋まり、後から来た人がパネルに表示されなくなります。プロセスを再起動すれば戻ります。
- **発言と直近30件の会話履歴は、翻訳のため OpenRouter 経由でモデルプロバイダに送信されます。**
  ログイン画面にもその旨を表示しています。
- **一応のプロンプトインジェクション対策はしてありますが完璧ではありません**。 AIによる訳文は不正確な可能性があります。原文を確認するには、発言に付いている言語バッジにマウスを乗せてください。

### 無料モデルについて

このアプリは既定で、OpenRouter の `data_collection: "deny"` を指定します。
**入力を学習に使いうる配信先へのルーティングを拒否する**という意味です
(OpenRouter はこれを指定しないと `"allow"` として扱います)。

無料エンドポイントには入力を学習に使うものが多く含まれます。そのため既定のままだと、無料モデルを
指定しても次のエラーで弾かれることがあります。

```
404 No endpoints found matching your data policy (Free model training)
```

このとき翻訳は行われず、発言は原文のまま表示されます(チャット自体は止まりません)。
理由はサーバーのログに出ます。

承知のうえで無料モデルを使うなら `OPENROUTER_ALLOW_TRAINING=1` を設定してください。
その場合、参加者の発言が学習データとして使われる可能性があります。**参加者にとっては
自分が同意していないデータ提供になる**ので、そこは運用する人の判断でお願いします。

なお、実際にどの配信先に回るかは OpenRouter のアカウント設定にも左右されます。
https://openrouter.ai/settings/privacy を一度確認しておくことを勧めます。
